<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <title>Document</title>
    <link rel="shortcut icon" href="/img/favicon.ico" type="image/x-icon">

</head>
<style>

    *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;

    }
    body{
        background-color: rgb(26, 23, 23);

    }
    header{
        display: flex;
        align-items: center;
        justify-content: space-between;
        border: 0;
        border-bottom: 1px solid goldenrod;
        padding: 30px;
    }
    nav ul{
        display: flex;
        gap: 20px;
        list-style: none;
    }
    .nav-item a{
        text-decoration: none;
        color: white;     
    }
    .nav-item{
        position: relative;
        font-size: 22px;
        font-family:Arial, Helvetica, sans-serif
    }
    .nav-item:after{
        content: '';
        position: absolute;
        height: 5px;
        width: 0px;
        background-color: white;
        border-radius: 10px;
        bottom: -2px;
        right: 0%;
        transition: 1s;
    }
    .nav-item:hover:after{
        width: 100%;
        left: 0%;   
    }
    footer{
        color: white;
        border: 0;
        border-top:1px solid goldenrod ;
        font-size: 20px;
        font-family: Arial, Helvetica, sans-serif;
        text-align: center;
        padding: 10px;
        line-height: 30px;
    }
    footer span{
        color: goldenrod;
    }
    /* Estilo base para a mensagem flash */
.flash-message {
    position: fixed;
    top: 15%;
    right: 10px;
    z-index: 1000;
    width: 400px;
}

/* Estilo do flash card */
.flash-card {
    background-color: rgb(255, 255, 255); /* Verde para sucesso */
    color: rgb(0, 0, 0);
    padding: 15px;
    margin-bottom: 10px;
    border-radius: 5px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    animation: fadeOut 5s forwards ease-in-out; /* Animação para desaparecer */
    text-align: center;
    }

/* Animação para desaparecer gradualmente */
@keyframes fadeOut {
    0% {
        opacity: 1;
    }
    50%{
        opacity: 1;
    }
    100% {
        opacity: 0;

    }
}

    
</style>
<body>
    <header>
        <a href="/thoughts"><img src="/img/toughts_logo.png" alt=""></a>
    <nav>
        <ul>
            <li class="nav-item"><a href="/">Pensamentos</a></li>

{{#if session.userid}}
    <!-- Se o usuário estiver logado (ou seja, a sessão contém um `userid`): -->
    <li class="nav-item">
        <a href="/thoughts">Postar Pensamentos</a> <!-- Link para postar pensamentos -->
    </li>
    <li class="nav-item">
        <a href="/logout">Sair</a> <!-- Link para logout -->
    </li>
{{else}}
    <!-- Se o usuário NÃO estiver logado: -->
    <li class="nav-item">
        <a href="/login">Entre</a> <!-- Link para a página de login -->
    </li>
    <li class="nav-item">
        <a href="/register">Crie sua conta</a> <!-- Link para a página de registro -->
    </li>
{{/if}}
        </ul>
    </nav>
    </header>
    <div class="flash-message">
    {{#if messages.message}}
        <!-- Se houver mensagens flash armazenadas em `messages.message` -->
        {{#each messages.message}}
            <!-- Itera sobre cada mensagem na lista de mensagens -->
            <div class="flash-card">{{this}}</div> <!-- Exibe a mensagem dentro de um cartão -->
        {{/each}}
    {{/if}}
</div>


    {{{body}}}
    <footer><span>Tougths</span> &copy2024</footer>
    <script src="/js/flash.js"></script>
</body>

</html>